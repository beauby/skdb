[package]
name = "skip-runtime"
version = "0.1.0"

[dependencies]
std = { path = "../prelude" }
skjson = { path = "../skjson" }

[dev-dependencies]
sktest = { path = "../sktest" }

[[bin]]
name = "skip-runtime"
main = "undefined"

